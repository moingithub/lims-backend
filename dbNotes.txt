1- Delete the existing lims database
2- Create new lims database
3- Delete the folder migrations which is under prisma
4. Migrate DB Structure
npx prisma migrate dev --name init
5- Execute script in db from prisma\additional-db-scripts.sql
6. Seed 
npx prisma db seed
or
node prisma/seed.js

-- To Update new Fields--
npx prisma migrate dev --name add_pop_fee_fields


--------For API Update----------------
-- How to upload changes in production
sudo pm2 list
-- Assume 0 is the API
sudo pm2 stop 0
rm -rf node_modules package-lock.json
upload changes from fileZilla ( Do not upload node_modules)
npm install
sudo pm2 start 0

--- In User company_id is not saved. Need to check that

For views, create view in db and then exectue the folowing in terminal. This will pull the view in prisma
npx prisma db pull